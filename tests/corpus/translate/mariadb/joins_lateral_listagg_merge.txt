# MariaDB lowerings for FULL OUTER JOIN, LATERAL, windowed LISTAGG, and MERGE.
SELECT p.name, t.name FROM people p FULL OUTER JOIN teams t ON p.team_id = t.id ORDER BY t.id, p.id => SELECT __dbsaci_foj_c0, __dbsaci_foj_c1 FROM (SELECT p.name AS __dbsaci_foj_c0, t.name AS __dbsaci_foj_c1, t.id AS __dbsaci_foj_o0, p.id AS __dbsaci_foj_o1 FROM PEOPLE p LEFT JOIN TEAMS t ON p.team_id = t.id UNION ALL SELECT p.name, t.name, t.id, p.id FROM PEOPLE p RIGHT JOIN TEAMS t ON p.team_id = t.id WHERE p.team_id IS NULL) __dbsaci_foj ORDER BY __dbsaci_foj_o0 IS NULL, __dbsaci_foj_o0, __dbsaci_foj_o1 IS NULL, __dbsaci_foj_o1
SELECT p.name, x.c FROM people p CROSS JOIN LATERAL (SELECT COUNT(*) c FROM people q WHERE q.team_id = p.team_id) x WHERE p.id = 1 => SELECT p.name, (SELECT COUNT(*) FROM PEOPLE q WHERE q.team_id = p.team_id) FROM PEOPLE p WHERE p.id = 1
SELECT id, LISTAGG(name, ',') WITHIN GROUP (ORDER BY id) OVER (PARTITION BY team_id) FROM people WHERE team_id = 1 ORDER BY id => SELECT id, (SELECT GROUP_CONCAT(__dbsaci_la.name ORDER BY __dbsaci_la.id SEPARATOR ',') FROM PEOPLE __dbsaci_la WHERE __dbsaci_la.team_id <=> __dbsaci_src.team_id) FROM PEOPLE __dbsaci_src WHERE team_id = 1 ORDER BY id
MERGE INTO mtgt d USING (SELECT 1 AS id, 'new' AS val FROM DUAL) s ON (d.id = s.id) WHEN MATCHED THEN UPDATE SET d.val = s.val => UPDATE MTGT d INNER JOIN (SELECT 1 AS id, 'new' AS val FROM DUAL) s ON (d.id = s.id) SET d.val = s.val
MERGE INTO mtgt d USING (SELECT 2 AS id, 'fresh' AS val FROM DUAL) s ON (d.id = s.id) WHEN NOT MATCHED THEN INSERT (id, val) VALUES (s.id, s.val) => INSERT INTO MTGT (id, val) SELECT id, val FROM (SELECT 2 AS id, 'fresh' AS val FROM DUAL) s WHERE NOT EXISTS (SELECT 1 FROM MTGT d WHERE d.id = s.id)
MERGE INTO mtgt d USING msrc2 s ON (d.id = s.id) WHEN MATCHED THEN UPDATE SET d.val = s.val WHEN NOT MATCHED THEN INSERT (id, val) VALUES (s.id, s.val) => INSERT INTO MTGT (id, val) SELECT id, val FROM MSRC2 s ON DUPLICATE KEY UPDATE val = VALUES(val)
# MATCHED UPDATE DELETE WHERE is covered by mariadb_tests::merge_matched_delete_where_becomes_update_then_delete_batch (multi-statement; not line-oriented).
